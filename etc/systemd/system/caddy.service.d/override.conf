[Service]
Restart=always
RestartSec=5s
# X-Edge-Proxy marker, generated per box by install.sh. The leading dash means
# "do not fail if absent" — a box with no marker still starts.
EnvironmentFile=-/etc/caddy/edge.env
