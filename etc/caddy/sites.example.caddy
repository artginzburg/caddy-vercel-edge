# Copy to sites/<domain>.caddy and edit. One file per project.
#
# Standard Vercel project (www -> apex 301, apex -> origin.<domain>):
import vercel example.com

# No www? Use the apex-only form instead of the snippet above:
#     example.com {
#         import logged
#         import edge https://origin.example.com
#     }
